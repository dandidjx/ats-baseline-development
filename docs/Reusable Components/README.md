# Reusable Components

This folder captures reusable UI/component guidance to avoid one-off patterns.

## Files
- `Reusable Components.md`: component catalog baseline and reuse guidance.

Use this folder when creating or updating shared UI parts so behavior and naming stay consistent across modules.
